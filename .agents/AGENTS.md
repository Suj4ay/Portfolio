# Workspace Agent Rules

- Automatically commit and push to GitHub (`origin main`) whenever meaningful code or asset changes are made.
- Use clear Conventional Commit messages (`feat:`, `fix:`, `docs:`, `style:`, `refactor:`).
